# ScopeLock Backend Dependencies
# Python 3.11+
#
# Minimal file-based backend: webhooks + Telegram + Claude CLI runner

# Web Framework (with pre-built wheels for Render)
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1

# HTTP Client (for Telegram API)
httpx==0.27.2

# Data & Parsing
python-multipart==0.0.12

# Mission Deck Dependencies
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
email-validator==2.3.0
requests==2.31.0
PyNaCl==1.5.0  # For Solana Ed25519 signature verification
base58==2.1.1  # For Solana address/signature decoding

# Compensation System Dependencies
websockets==12.0  # For WebSocket real-time updates
